{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(postEditForm, {'attr': {'novalidate': 'novalidate'}}) }}
    {{ form_widget(postEditForm) }}
    <input type="submit" value="Update" class="btn btn-success">
    {{ form_end(postEditForm) }}
{% endblock %}